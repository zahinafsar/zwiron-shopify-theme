{%- liquid
  assign collection = section.settings.collection
  assign max = section.settings.max_products | default: 5
  if max > 5
    assign max = 5
  endif
  assign cols = section.settings.columns | default: 5
  assign fallback_imgs = 'https://images.pexels.com/photos/15028227/pexels-photo-15028227.jpeg?auto=compress&cs=tinysrgb&h=650&w=940|https://images.pexels.com/photos/28481966/pexels-photo-28481966.jpeg?auto=compress&cs=tinysrgb&h=650&w=940|https://images.pexels.com/photos/11515541/pexels-photo-11515541.jpeg?auto=compress&cs=tinysrgb&h=650&w=940|https://images.pexels.com/photos/19644204/pexels-photo-19644204.jpeg?auto=compress&cs=tinysrgb&h=650&w=940|https://images.pexels.com/photos/30181058/pexels-photo-30181058.jpeg?auto=compress&cs=tinysrgb&h=650&w=940' | split: '|'
  assign fallback_names = 'Lumen Trench|Volta Knit|Aero Cargo|Strata Boot|Helix Coat' | split: '|'
  assign last_index = max | minus: 1
-%}

<section class="zw zw-section zw-products" id="zwiron-products">
  <div class="zw-container">
    <header class="zw-section__head">
      <div>
        <div class="zw-section__eyebrow">{{ section.settings.eyebrow | default: '/ 02 — In stock now' }}</div>
        <h2 class="zw-section__title">{{ section.settings.title_1 | default: 'Featured' }} <em>{{ section.settings.title_2 | default: 'pieces.' }}</em></h2>
      </div>
      <a href="{{ section.settings.link_url | default: routes.collections_url }}" class="zw-section__link">{{ section.settings.link_label | default: 'View all →' }}</a>
    </header>

    <div class="zw-products__grid" style="--zw-cols: {{ cols }};">
      {%- if collection.products.size > 0 -%}
        {%- for product in collection.products limit: max -%}
          {%- assign idx = forloop.index0 | modulo: 5 -%}
          {% render 'zwiron-product-card', product: product, fallback_image: fallback_imgs[idx], fallback_name: product.title %}
        {%- endfor -%}
      {%- else -%}
        {%- for i in (0..last_index) -%}
          {% render 'zwiron-product-card', fallback_image: fallback_imgs[i], fallback_name: fallback_names[i] %}
        {%- endfor -%}
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Zwiron Products",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "/ 02 — In stock now" },
    { "type": "text", "id": "title_1", "label": "Title", "default": "Featured" },
    { "type": "text", "id": "title_2", "label": "Title (italic)", "default": "pieces." },
    { "type": "collection", "id": "collection", "label": "Collection" },
    { "type": "range", "id": "max_products", "label": "Max products", "min": 1, "max": 5, "step": 1, "default": 5 },
    { "type": "range", "id": "columns", "label": "Columns", "min": 2, "max": 5, "step": 1, "default": 5 },
    { "type": "url", "id": "link_url", "label": "View-all link" },
    { "type": "text", "id": "link_label", "label": "Link label", "default": "View all →" }
  ],
  "presets": [{ "name": "Zwiron Products" }]
}
{% endschema %}
